@model IEnumerable<Nieruchomosci.Models.NieruchomoscPhoto>

@{
    ViewBag.Title = "Gallery1";
}

<h2>Gallery1</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NieruchomoscID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ImageID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ImageSize)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FileName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NieruchomoscID)
        </td>
        <td>
          @* <img src="data:image/jpg;base64,@(Convert.ToBase64String(item.ImageData1))" alt="" />  też działa*@
            <img src = "@Url.Action("GetImage", "Nieruchomosci", new { id = item.ImageID })" />

          
          
        </td>
        @*<td>
        @Html.DisplayFor(modelItem => item.ImageData)
    </td>*@
        <td>
            @Html.DisplayFor(modelItem => item.ImageSize)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FileName)
        </td>
        

        <td>
            @Html.ActionLink("Edit", "Upload", new {  id=item.NieruchomoscID }) |
            @Html.ActionLink("Details", "Upload", new { id = item.NieruchomoscID }) |
            @Html.ActionLink("Delete", "Upload", new {  id=item.NieruchomoscID })
        </td>
    </tr>
}

</table>
